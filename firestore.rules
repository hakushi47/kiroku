rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // テスト用：一時的にすべての読み書きを許可
    // 本番環境では適切なセキュリティルールに変更してください
    match /{document=**} {
      allow read, write: if true;
    }
  }
} 